services:
  cosyvoice:
    image: zhufudea/cosyvoice-docker:latest
    container_name: cosyvoice-persion
    restart: unless-stopped
    ports:
      - "0.0.0.0:${PORT:-8188}:8188"
    volumes:
      - ${MODEL_PATH:-/data/project-dev/cosyvoice-docker/pretrained_models/Fun-CosyVoice3-0.5B}:/app/pretrained_models/Fun-CosyVoice3-0.5B
      - /tmp/cosyvoice/input:/data/input
      - /tmp/cosyvoice/output:/data/output
      - /tmp/cosyvoice/voices:/data/voices
    environment:
      - MODEL_DIR=${MODEL_DIR:-pretrained_models/Fun-CosyVoice3-0.5B}
      - PORT=8188
      - GPU_IDLE_TIMEOUT=${GPU_IDLE_TIMEOUT:-600}
      - NVIDIA_VISIBLE_DEVICES=0
      - CUDA_VISIBLE_DEVICES=0
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0"]
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8188/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
